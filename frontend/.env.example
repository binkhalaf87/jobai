# Public base URL for the backend API consumed by the frontend.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Production should normally call the backend through the same Vercel origin:
# /api/v1 -> BACKEND_URL/api/v1 via next.config.mjs rewrites.
BACKEND_URL=http://localhost:8000
